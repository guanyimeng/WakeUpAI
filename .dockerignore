# .dockerignore

# Git files
.git
.gitignore

# Docker files
.dockerignore
Dockerfile # Usually not needed inside the image itself unless for multi-stage builds referencing it

# Python virtual environment (if created in project dir)
.venv
venv/
ENV/
env/

# Python caches
__pycache__/
*.pyc
*.pyo
*.pyd

# Poetry files (poetry.lock and pyproject.toml are copied separately earlier in Dockerfile)
# poetry.lock
# pyproject.toml

# IDE and OS specific files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.DS_Store
Thumbs.db

# Test files and caches
tests/
.pytest_cache/
htmlcov/
.coverage
*.log # General log files, unless you want specific app logs in the image

# Temporary files / build artifacts
*.tmp
*.bak
*.swp

# Environment files (should be passed at runtime, not built into image)
.env
.env.*
!/.env_example # Keep .env_example if you want it in the image as a reference

# Test audio output directory (if created locally)
test_audio_output/
temp_alarm_audio/ # If this is meant for runtime, it should be a volume mount

# Specific local test files if any
test_alarms.json
test_alarms_unit.json
